{
  "name": "nuxt-to-githubpages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt-ts build",
    "start": "nuxt-ts start",
    "export": "nuxt-ts export",
    "serve": "nuxt-ts serve",
    "generate": "nuxt-ts build && nuxt-ts export",
    "deploy:pages": "push-dir --dir=dist/spa --remote=pages --branch=master"
  },
  "dependencies": {
    "@nuxt/content": "^1.3.2",
    "@nuxt/typescript-runtime": "^0.4.10",
    "@nuxtjs/axios": "^5.11.0",
    "@nuxtjs/pwa": "^3.0.0-beta.20",
    "nuxt": "^2.13.0",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^1.0.3",
    "@nuxtjs/vuetify": "^1.11.2"
  }
  
}
